Differences from presentation:
- File structure consolidated into load_balance.c/load_balance.h (our algorithms) and main.c (everything else)
- Timing simplified: only record LB + insertion sorting time on proc 0
Other code updates:
- Fixed bug in pivoting that left last proc empty
- Fixed bug in LB that led to inaccurate distribution
